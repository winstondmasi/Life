#functions for single cells in the program 

class Cell:
    # attributes
    def __init__(self, alive, row, col):
        self.alive = alive  # True if cell is alive, False if dead
        self.row = row      # row index on the grid
        self.col = col      # column index on the grid

   
        

